@model Projeto_PetShop.Models.Servico
@{
    ViewData["Title"] = "Deletar Serviço";
}

<div class="container mt-5">
    <div class="card shadow-lg rounded p-4 mx-auto" style="max-width: 600px;">
        <h2 class="text-center text-danger mb-4">Deletar Serviço</h2>

        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger text-center">
                @ViewBag.ErrorMessage
            </div>
        }

        <div class="text-center mb-4">
            <h5>Tem certeza que deseja excluir este serviço?</h5>
            <p class="text-muted">Após a exclusão, todos os dados referentes a este serviço serão perdidos.</p>
        </div>

        <div class="border rounded p-3 bg-light">
            <p><strong>📝 Nome do Serviço:</strong> @Model.Nome</p>
            <p><strong>💰 Valor:</strong> @Model.Valor.ToString("C")</p>
        </div>

        <form asp-action="Delete" method="post" class="text-center mt-4">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger me-2">Confirmar Exclusão</button>
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</div>
